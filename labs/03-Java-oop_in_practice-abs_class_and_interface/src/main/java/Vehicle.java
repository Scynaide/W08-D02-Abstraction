/**
 * Our Vehicle interface! As an interface, this is just a contract (or a blueprint) to specify what a class that
 * implements this interface should do.
 */
public interface Vehicle {

    String start();

    String stop();

    String drive();

    String changeGear();
}
